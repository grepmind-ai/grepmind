{
  "name": "grepmind-workspace",
  "version": "0.1.0-alpha",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "Grepmind packages",
  "type": "module",
  "scripts": {
    "build": "npm run build:agent-rpc && npm -w @grepmind/agent run build && npm -w @grepmind/mcp run build && npm run build:grepmind",
    "build:agent-rpc": "npm -w @grepmind/agent-rpc run build",
    "build:agent": "npm run build:agent-rpc && npm -w @grepmind/agent run build",
    "build:mcp": "npm run build:agent-rpc && npm -w @grepmind/mcp run build",
    "build:grepmind": "npm -w grepmind run build",
    "clean": "npm -ws run clean --if-present",
    "grepmind": "tsx packages/grepmind/src/index.ts",
    "agent": "npm -w @grepmind/agent run dev:cli --"
  },
  "author": "Zaytra",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zaytra-labs/grepmind.git"
  },
  "homepage": "https://github.com/zaytra-labs/grepmind#readme",
  "bugs": {
    "url": "https://github.com/zaytra-labs/grepmind/issues"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsdown": "^0.21.10",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
