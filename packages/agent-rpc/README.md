# @grepmind/agent-rpc

Typed client for the local Grepmind agent runtime socket.

The package exposes the same public runtime operations that are available through
the agent CLI, without importing the full agent runtime implementation.
